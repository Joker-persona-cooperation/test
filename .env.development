# 开发环境 API 地址统一走本地 Vite 代理，代理再转发到后端 API。
# 这样浏览器侧始终保持同源请求，refresh/csrf Cookie 才能参与无感刷新链路。
VITE_API_BASE_URL=/api/v1

# Vite 代理目标：本地后端（默认 8888）。
# 切到服务器 dev 请运行 `npm run dev:remote`（见 .env.remote）。
TASKPILOT_DEV_API_TARGET=http://127.0.0.1:8888
